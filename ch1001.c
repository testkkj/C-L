#include <stdio.h>
main()
{
    int ctr = 0;

    ctr = ctr + 1; // 카운터를 1로 증가한다.
    printf("카운터의 값은 %d.\n", ctr);
    ctr = ctr + 1; // 카운터를 2로 증가한다.
    printf("카운터의 값은 %d.\n", ctr);
    ctr = ctr + 1; // 카운터를 3로 증가한다.
    printf("카운터의 값은 %d.\n", ctr);
    ctr = ctr + 1; // 카운터를 4로 증가한다.
    printf("카운터의 값은 %d.\n", ctr);
    ctr = ctr + 1; // 카운터를 5로 증가한다.
    printf("카운터의 값은 %d.\n", ctr);
    ctr = ctr - 1; // 카운터를 4로 감소한다.
    printf("카운터의 값은 %d.\n", ctr);
    ctr = ctr - 1; // 카운터를 3로 감소한다.
    printf("카운터의 값은 %d.\n", ctr);
    ctr = ctr - 1; // 카운터를 2로 감소한다.
    printf("카운터의 값은 %d.\n", ctr);
    ctr = ctr - 1; // 카운터를 1로 감소한다.
    printf("카운터의 값은 %d.\n", ctr);

    return 0;
}
